[package]
name = "scirust"
version = "0.1.0"
description = "SciPy-inspired numerical routines."
edition.workspace = true
license.workspace = true
rust-version.workspace = true

[dependencies]
numrs-core = { path = "../numrs-core" }
